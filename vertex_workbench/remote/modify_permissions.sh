# Execute this command via ssh on the google cloud ui 
# Navigate to workbench instance > Click on "View in Compute Engine" > Click on "SSH" > Enter the command below

sudo chmod -R 777 /home/jupyter
